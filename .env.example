# Copy this file to .env.local (gitignored) and set the complete backend origin.
# Do not include a trailing slash. Restart `npm run dev` after changing it.
# Production builds use .env.production instead.
VITE_BACKEND_URL=https://demo1.ecuenta.online
